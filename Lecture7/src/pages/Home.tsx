import React from 'react';
import { Typography } from '@mui/material';

const Home: React.FC = () => (
    <Typography variant="h4">Welcome to the Rick & Morty App!</Typography>
);

export default Home;
